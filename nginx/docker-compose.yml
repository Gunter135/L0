version: '3.8'

services:
  nginx:
      image: nginx:latest
      ports:
        - "80:80"
      volumes:
        - ./nginx.conf:/etc/nginx/nginx.conf
      networks:
        - backend-net
networks:
  kafka-net:
    external: true
  backend-net:
    external: true